platform=android
env=local
retry.count=1
video.recording.enabled=false
video.save.on.pass=false
# Optional: attach a screenshot after every @Step to Allure + Extent (slower).
screenshot.each.step=false
auto.start.server=false
default.wait.timeout=10
